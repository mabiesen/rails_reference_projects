<%= form_with(model: page, local: true) do |form| %>
  <% if page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(page.errors.count, "error") %> prohibited this page from being saved:</h2>

      <ul>
      <% page.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :page_name %>
  </div>

  <div class="field">
    <%= form.label :text %>
    <%= form.text_area :text, id: :page_text %>
  </div>

  <div class="field">
    <%= form.label :type %>
    <%= form.text_field :type, id: :page_type %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
